apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: test-sidecar-pipelinerun
spec:
  serviceAccountName: build-sa
  pipelineRef:
    name: test-sidecar-pipeline
  resources:
  - name: cnych-res  # 指定输入的git仓库资源
    resourceRef:
      name: cnych-res
  params:
  - name: image
    value: registry.cn-hangzhou.aliyuncs.com/cxs1103/tekton-demo
